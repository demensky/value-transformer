import type {OffsetCounter} from '../base/offset-counter';

export function resizableUintFromDataView(
  _view: DataView,
  _offset: OffsetCounter,
): number {
  throw new Error('Not implemented');
}
